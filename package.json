{
  "devDependencies": {
    "@playwright/test": "^1.33.0",
    "allure-playwright": "^2.2.1",
    "playwright": "^1.33.0"
  },
  "scripts": {
    "test": "npx concurrently \"flask run\" \"npx playwright test\""
  },
  "dependencies": {
    "wait-on": "^7.0.1"
  }
}
